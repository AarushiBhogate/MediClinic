@model MediClinic.Models.Schedule

@{
    ViewData["Title"] = "Edit Schedule";
}

<div class="container my-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-lightblue text-dark fw-bold fs-4">
            Edit Schedule
        </div>
        <div class="card-body bg-white">
            <form asp-action="EditSchedule" class="row g-3">
                <input type="hidden" asp-for="ScheduleId" />

                <div class="col-md-6">
                    <label asp-for="PhysicianId" class="form-label">Physician</label>
                    <select asp-for="PhysicianId" class="form-select" asp-items="ViewBag.PhysicianId"></select>
                    <span asp-validation-for="PhysicianId" class="text-danger"></span>
                </div>

                <div class="col-md-6">
                    <label asp-for="AppointmentId" class="form-label">Appointment</label>
                    <select asp-for="AppointmentId" class="form-select" asp-items="ViewBag.AppointmentId"></select>
                    <span asp-validation-for="AppointmentId" class="text-danger"></span>
                </div>

                <div class="col-md-6">
                    <label asp-for="ScheduleDate" class="form-label">Schedule Date</label>
                    <input asp-for="ScheduleDate" type="date" class="form-control" />
                    <span asp-validation-for="ScheduleDate" class="text-danger"></span>
                </div>

                <div class="col-md-6">
                    <label asp-for="ScheduleTime" class="form-label">Schedule Time</label>
                    <input asp-for="ScheduleTime" type="time" class="form-control" />
                    <span asp-validation-for="ScheduleTime" class="text-danger"></span>
                </div>

                <div class="col-12 d-flex justify-content-start mt-3">
                    <button type="submit" class="btn btn-lightblue me-2">Save</button>
                    <a asp-action="GetSchedule" class="btn btn-secondary">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    body {
        background-color: #f0f8ff; /* very light blue background */
        font-family: Arial, sans-serif;
    }

    .bg-lightblue {
        background-color: #cce7ff !important; /* soft light blue */
    }

    .btn-lightblue {
        background-color: #5dade2;
        color: white;
        border: none;
    }

        .btn-lightblue:hover {
            background-color: #4a9edc;
        }

    .form-label {
        font-weight: 500;
        color: #1a1a1a;
    }

    .card {
        max-width: 900px;
        margin: auto;
        border-radius: 10px;
    }
</style>
